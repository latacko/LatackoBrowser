namespace GraphicsCore;

public enum CursorType : byte
{
    defaultCursor,
    pointer,
    text,
    move,
    wait,
    cursorHelp,
    notAllowed,
    progress,
    crosshair,
    grab,
    grabbing,
    none,
}